import React from "react";

import { TakeawayPage } from "containers";

import { Layout, SEO } from "components";

const Takeaway = () => (
  <Layout>
    <SEO title="Takeaway" />
    <TakeawayPage />
  </Layout>
);

export default Takeaway;
